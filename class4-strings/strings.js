// Strings examples
let singleQuoteString = 'This is a single-quoted string'; // Single quotes
let doubleQuoteString = "This is a double-quoted string"; // Double quotes
let templateString = `This is a template string with a variable: ${singleQuoteString}`; // Template literal

console.log(singleQuoteString);
console.log(doubleQuoteString);
console.log(templateString);

// String manipulations
let jsString = "JavaScript es lo máximo";
console.log(jsString.toLowerCase()); // Lowercase
console.log(jsString.toUpperCase()); // Uppercase
console.log(jsString.substring(0, 10)); // Substring to extract "JavaScript"
